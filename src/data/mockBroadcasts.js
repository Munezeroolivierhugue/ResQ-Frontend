// TODO: backend gap — GET/POST /api/broadcasts exist but no creation UI built yet.
// When broadcast UI is implemented, replace with src/api/broadcasts.js.
/** Broadcast records pushed by OM screens (map, multi-agency, escalation). */
export const mockBroadcasts = []
