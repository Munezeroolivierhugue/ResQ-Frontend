// PARTIAL INTEGRATION: listBroadcasts() and createBroadcast() wired to backend GET/POST /api/broadcasts.
/** Broadcast records pushed by OM screens (map, multi-agency, escalation). */
export const mockBroadcasts = []
