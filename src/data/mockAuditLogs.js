// TODO: backend gap — GET /api/admin/audit-logs exists but Analyst portal viewer not yet built.
/** Audit log records written when a field responder flags a dispatch issue. */
export const mockAuditLogs = []
